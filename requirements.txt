# torch is installed separately in Dockerfile with the correct CUDA wheel --
# listing it here with no version constraint prevents pyannote from reinstalling it from PyPI
torch
torchaudio
fastapi>=0.110.0
uvicorn>=0.27.0
python-multipart>=0.0.9
# pyannote 4.x is the only line compatible with modern huggingface_hub
# (3.x still calls hf_hub_download with the removed use_auth_token kwarg).
# 4.x has a known VRAM regression during the diarize step
# (pyannote-audio#1963, still open) — peak ~6 GB instead of ~1 GB. We
# accept that here because our lazy-load + idle-release pattern means
# this is a transient peak, not a continuous hold.
pyannote.audio>=4.0.4
numpy>=1.24.0
scipy>=1.11.0
soundfile>=0.12.0
