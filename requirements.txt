# torch is installed separately in Dockerfile with the correct CUDA wheel --
# listing it here with no version constraint prevents pyannote from reinstalling it from PyPI
torch
torchaudio
fastapi>=0.110.0
uvicorn>=0.27.0
python-multipart>=0.0.9
# pyannote pinned to the 3.x line — 4.0.3 has an open VRAM regression
# (pyannote-audio#1963). Bump to 4.x only after that lands a fix.
pyannote.audio>=3.1.1,<4.0
numpy>=1.24.0
scipy>=1.11.0
soundfile>=0.12.0
